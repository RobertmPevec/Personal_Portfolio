---
import { info } from "@data";

import ProjectsWrapper from "@components/general/ProjectsWrapper.astro";
import MetaHead from "@components/general/MetaHead.astro";
import Layout from "@layouts/Layout.astro";
import "../styles/main.css";
import "../styles/blog.css";

const Projects = info.projects.map((project) => project);
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <MetaHead
      title={"Blog"}
      description={"Update Blog posts on this page"}
      ogImageUrl={"/assets/images/profile.png"}
    />
  </head>

  <body class="bg-light dark:bg-dark text-black dark:text-white">
    <!-- Canvas Background -->
    <!-- Main Content -->
    <Layout>
      <h1 style="line-height: 1.2;">Blog</h1>
      <div>
        <div class="grid">
          <a href="/blogpage-011125/">
            <h2 class="block text-sm text-black dark:text-white" id="heading">Finance Tracker Website with Plaid API finished</h2>
            <img src="/assets/images/finance-tracker.png" alt="Test Image">
            <h3 class="block text-sm text-black dark:text-white" id="datetime">January 1, 2025</h3>
          </a>
          <p class="block text-sm font-medium text-black dark:text-white">After starting this finance tracker app 4-5 months ago, I’m finally done! School got in the way for a while, but I spent this past week focusing on it and was able to wrap everything up. It feels good to have a finished product that works the way I envisioned, and it’s been a great way to apply some of the skills I’ve been learning. You can check it out <a href="financeflowtech.com/home">here</a>.</p>
        </div>
        <div class="grid">
          <a href="/blogpage-010125/">
            <h2 class="block text-sm text-black dark:text-white" id="heading">2025 Programming Goals and my Introduction Into Machine Learning</h2>
            <img src="/assets/images/machinelearning.png" alt="Test Image">
            <h3 class="block text-sm text-black dark:text-white" id="datetime">January 1, 2025</h3>
          </a>
          <p class="block text-sm font-medium text-black dark:text-white">Over the winter break, I decided to try something new and started Andrew Ng's Machine Learning Specialization. I didn’t expect it to become such an eye-opening experience, but it’s been a great introduction to machine learning and how it applies to the things I’m already learning in my math and computer science degree.</p>
        </div>
    </Layout>
    <canvas id="starry-background"></canvas>

    <!-- Scripts -->
    <script src="/assets/starry-background-js.js" defer></script>
    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const progress = document.getElementById("progressbar");
    
        function updateProgressBar() {
          const scrollTop = window.scrollY || document.documentElement.scrollTop;
          const totalHeight =
            document.documentElement.scrollHeight - document.documentElement.clientHeight;
          const progressHeight = (scrollTop / totalHeight) * 100;
    
          // Update the progress bar height
          progress.style.height = progressHeight + "%";
        }
    
        // Attach scroll and resize events
        window.addEventListener("scroll", updateProgressBar);
        window.addEventListener("resize", updateProgressBar);
    
        // Initialize the progress bar on page load
        updateProgressBar();
      });
    </script>
  </body>
</html>